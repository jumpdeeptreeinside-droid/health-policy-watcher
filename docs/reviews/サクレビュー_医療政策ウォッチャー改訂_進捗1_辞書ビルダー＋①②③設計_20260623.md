# サクレビュー：医療政策ウォッチャー改訂 進捗①（辞書ビルダー＋①②③設計）2026-06-23

サクマの進捗①（cfeb102）をレビュー。**総評：④辞書ビルダー＝合格（条件3点・全て次回対応可）。①②③設計＝承認方向。** サクマの安全判断（鍵なしで本番パイプラインを無検証改変せず①②③を設計にとどめた／辞書は独立モジュールで非破壊／人手読み温存）は正しい。「設計先・実装後」のキャッチボールでいく。

## ④ 辞書ビルダー（`voicepeak_dict_builder.py`）＝合格・条件3点
**コードで確認した良い点**
- 🔴**人手読み温存**：`merge_entries` L82-84＝既存surはpron温存でskip。faithfulnessガード（薬局func_*のunknown・患者調査の秘匿NULLと同じ思想）。コードで裏取り済。
- 独立・非破壊（podcast_dictionary.jsonは孤立ファイル）／dry-run／Gemini任意（temperature0.1・頑健パース）／保存形式維持。

**🔴条件1（最重要・読みの正確性）：Geminiの読みは"人（緑さん）の確認"を必須に**
- `update_dictionary_from_script(use_llm=True)` はGemini生成読みを**そのままmerge→save**（L189-192）。Geminiは医療・政策の専門用語/機関名/人名の読みを**誤りうる**。誤読が辞書に入ると、その語が**常に間違って読まれる＝体系的誤読**（緑さんの都度修正より悪い）。
- → 本番フックでは**自動saveしない**。`--dry-run`／候補txtで「**緑さんが読みを確認→承認したものだけ本採用**」のステップを必須に（緑さん＝実際に音声を聞く人＝読みの正否を判断できる唯一の人）。最初の数回は全件確認、慣れたら差分のみ。サクマの「更新はPR/コミットに出して人がレビュー」を**コードの既定動作**に落とす（use_llm=True直saveは本番配線で使わない）。

**⚠️条件2：オフライン漢字候補のノイズを絞る**
- `_RE_KANJI`（L40）は2字以上の連続漢字を全部拾う＝医療政策原稿では「医療」「政策」「会議」等の**読み自明な普通名詞が大量混入**（--no-llmの候補txtがノイズだらけに）。
- → オフラインは`include_kanji=False`を既定にして難読漢字はGeminiパスに委ねる、or 頻出普通名詞のストップワード/低頻度フィルタを足す。

**⚠️条件3：数字+単位（`_RE_NUMUNIT`）は辞書化から外す方向で相談**
- 「2日間」「5疾病」を登録しても、数字が変われば(3日間/6疾病)別エントリ要＝**辞書膨張＆カバー不能**。VOICEPEAK辞書（固定sur→固定pron）は可変の数字と相性が悪い。
- → 辞書は**固有名詞・略語・難読語（不変のもの）に集中**。数字+単位の読み揺れは原稿生成側で吸収（台本に「ふつかかん」等）する方が筋。

**nit**：`merge_entries` L88 は`{e["sur"] for e in existing}`をループ内で再構築＝O(n²)。候補少で実害小だが、setを1回作って使い回しを。pron空の既存エントリにGemini読みを入れたい場合は別途（現状は温存優先でskip＝設計判断としてOK）。

## ①②③ 設計レビュー＝承認方向
- **① Notion優先度（高/低）＋国内国際はURLドメイン判定流用**＝Notionに国際フラグを増やさず既存`is_international()`を使う＝良い。未設定既定「低」（安全側＝厚くしすぎない）＝良い。
- **② 生成分岐（高=厚め含意/低=一行ヘッドライン`PROMPT_BLOG_BRIEF`）**＝良い。**ステータス文字列の大文字/小文字揺れ（執筆待ち(pdf) vs (PDF)）の指摘は鋭い**＝分岐実装時に必ず統一（揺れはバグ源）。
- **③ デイリー統合（weekly_policy_report.pyを雛形）**＝実績パターン流用で堅実。**二重生成回避（②=素材生成／③=束ねるだけ・個別を再生成しない）＝設計の肝で正しい**。目標尺12-15分を先に決め高の厚さ×本数で調整＝依頼どおり。統合台本確定後に④辞書更新を呼ぶ＝本丸の自動化、良い。

## サクマの3つの問いへの回答
1. **辞書ビルダーの型・配線**：合格。ただし上記🔴条件1（Gemini読みは緑さん確認を既定動作に・自動saveしない）を満たす配線で。
2. **③のStatus(Podcast)波及（個別→日次ダイジェスト単位）＝本体で最も影響大。設計方針**：**「音声化待ち」の単位を"記事"から"日次ダイジェスト"に移す**。個別記事はStatus(Podcast)を持たせず（or「統合対象」フラグのみ）、**統合台本の子ページ1つにStatus(Podcast)=音声化待ちを立てる**＝緑さんはその1本だけ音声化。automation L249の波及を日次ダイジェストページに張り替え。移行は**フィーチャーフラグで個別↔日次を切替**できる形にし、既存個別フローと混在させない（一気に置換せず1日テストしてから）。
3. **進め方（設計承認後＋テスト鍵環境で1回通してから本番化）**：**完全同意。鉄則。** 無検証で本番Actionsに載せない。CI or 木内さんの手元（鍵あり）で1回通す→再レビュー→本番。

## 次の一歩（推奨順）
1. **辞書ビルダーを実台本1本で試す**（鍵のある環境）：`--no-llm`で候補確認→`--llm --dry-run`でGemini読みを目視→緑さんが読みを確認→本採用。条件2(漢字ノイズ)・条件3(数字単位)はこの実走で量を見て判断。
2. ①②③本体実装は、この設計＋Q2のStatus波及方針を木内さん承認後、テスト鍵環境で1回通してから。
3. 体調最優先（橋本先生「ゆっくり」）＝元気な日に。

**判定：④合格（条件3点・次回可）／①②③設計承認。Gemini読みの人確認だけは本番前に必ず。** — サク
