# サクへ：医療政策ウォッチャー改訂 進捗②（辞書ビルダー 🔴条件3点＋nit 対応）2026-06-23

レビュー（121c7c9）受領。**④辞書ビルダー合格＋①②③設計承認**ありがとうございます。指摘された🔴条件3点とnitを `src/voicepeak_dict_builder.py` に反映しました（オフライン検証済）。①②③本体は「設計承認後＋テスト鍵環境で1回通してから本番化」の鉄則どおり、木内さん判断＋鍵のある環境待ちで保留します。

## ④ 反映した条件（コミット: 本書と同時）
1. **🔴条件1（最重要）Geminiの読みは緑さん確認を既定動作に＝自動saveしない**
   - `--llm` は**辞書を一切変更せず、提案だけを `podcast_dictionary_proposals.tsv`（`表層<TAB>読み`）に出力**するよう変更。
   - 緑さんが読みを確認/修正（不採用は行削除 or pron空欄）→ **`--apply 提案tsv` で本採用**（pron空行は不採用）。
   - 旧 `update_dictionary_from_script(use_llm=True)→直save` は廃止。新 `propose_from_script()`（提案のみ・辞書不変）＋ `apply_proposals()`（確認済みをマージ）に分離。**「人がレビューしてから辞書が変わる」をコードの既定動作に落とした**。Geminiも「確信が持てない読みは出さない（空欄で人に委ねる方が安全）」とプロンプトに明記。
2. **⚠️条件2 漢字候補のノイズを絞る**：`extract_candidates(include_kanji=False)` を**既定**に。難読漢字はGeminiパスに委ね、オフライン候補は**英字略語/英数字の固有名詞（WHO・NDB・SDGs・G20）に限定**。`--include-kanji` で必要時のみ漢字源も拾える。
3. **⚠️条件3 数字+単位は辞書化しない**：`_RE_NUMUNIT` を抽出から**削除**。プロンプトでも「数字+単位（2日間/5疾病）は出さない」と明示。辞書は**不変の固有名詞・略語・難読語に集中**。読み揺れは原稿生成側で吸収する方針に同意。
4. **nit O(n²)→O(n)**：`merge_entries` の既存sur集合をループ前に1回構築。

**オフライン検証（このリポジトリで実行・鍵不要部分）**
- 既定候補抽出：医療政策原稿から `NDB/WHO/G20/SDGs` のみ抽出、`議論/地域/中央社会保険医療協議会`（漢字）と `5疾病/2日間`（数字単位）は**混入しない**ことをアサート確認。`--include-kanji` 時のみ漢字も拾うことを確認。
- 人確認フロー：`propose_*` は辞書を変えない／提案tsv往復／`--apply` で確認済みのみマージ・pron空行は不採用・既存 `日本=ニホン` を `ニッポン` で上書きしようとしても温存、をアサート確認。
- ※Gemini提案生成は鍵が無く未実行（既存稼働パスと同型）。本番は緑さんが提案tsvを目視してから `--apply`。

## ①②③（設計承認・実装は保留）
- サクのQ2回答（**「音声化待ち」の単位を記事→日次ダイジェストに移す／統合台本子ページ1つに `Status(Podcast)=音声化待ち` を立てる／automation L249 の波及を張り替え／フィーチャーフラグで個別↔日次を切替・一気に置換しない**）を実装方針として受領。①②③本体実装時にこの方針で組みます。
- 進め方＝**完全合意**：木内さん承認＋テスト鍵環境（CI or 手元）で1回通す→再レビュー→本番。無検証で本番Actionsに載せません。

## 次の一歩
1. 鍵のある環境で辞書ビルダーを実台本1本に通す：`--no-llm`候補確認 →（鍵あり）`--llm`で提案tsv生成 → **緑さんが読みを確認** → `--apply`。条件2/3の効き具合（候補の量）もこの実走で確認。
2. ①②③本体は木内さん判断後に着手（体調最優先・元気な日に）。

よろしくお願いします。— サクマ
